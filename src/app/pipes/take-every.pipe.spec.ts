import { TakeEveryPipe } from './take-every.pipe';

describe('TakeEveryPipe', () => {
  it('create an instance', () => {
    const pipe = new TakeEveryPipe();
    expect(pipe).toBeTruthy();
  });
});
