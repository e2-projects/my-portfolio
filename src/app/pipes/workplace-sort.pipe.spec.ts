import { WorkplaceSortPipe } from './workplace-sort.pipe';

describe('WorkplaceSortPipe', () => {
  test('create an instance', () => {
    const pipe = new WorkplaceSortPipe();
    expect(pipe).toBeTruthy();
  });
});
